.beads/beads.db*
.beads/daemon.lock
.beads/daemon.log
.beads/last-touched

# Agent/orchestrator runtime
.agent/
.logs/
mcp_agent_mail/
ralph.yml

# Build / output
/build/
/build-host/
/build-test/
/builddir/
/out/

# Local Python packages
/python/

# Renode local app bundle (optional; not tracked)
renode/*.app/
renode/output/

# In-source local host builds (avoid polluting the tree)
tests/host/host_sim
tests/host/host_tests
tests/host/host_tests_ui

# Firmware build artifacts
*.o
*.elf
*.map

# Tooling caches
__pycache__/
.pytest_cache/
.venv/
.bv/
.cache/

# Binary dumps (debugging)
*.bin

# Beads daemon artifacts
.beads/daemon-error

# macOS
.DS_Store
